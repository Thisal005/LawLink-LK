// tailwind.config.js
export const theme = {
    extend: {
        colors: {
            primary: 'var(--primary)',
            'primary-dark': 'var(--primary-dark)'
        }
    }
}

  // tailwind.config.js
extend: {
    animation: {
      'rotate-slow': 'rotate 60s linear infinite'
    }
  }